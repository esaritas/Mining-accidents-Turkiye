# Copy to .env and adjust. No secrets are required for the foundation build.

# Path to the SQLite database created by `make db`.
MINING_ACCIDENTS_DB=database/mining_accidents.sqlite

# Optional: fixed epoch for reproducible exports (see export.py).
# SOURCE_DATE_EPOCH=
