# data/raw — immutable raw evidence store

Original retrieved documents, exactly as obtained, named by sha256 content
hash. **Never committed to git** (see .gitignore), never modified, never
deleted. May contain unredacted personal data — nothing here is ever exported
directly (see docs/privacy_and_persons_protocol.md).
