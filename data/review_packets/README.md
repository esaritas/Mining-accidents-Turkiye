# data/review_packets — review packets

Markdown packets generated by packets.py for human reviewers (see
docs/manual_review_protocol.md). The PILOT-01..PILOT-12 templates are EMPTY
slots annotated only with a selection criterion — populating them requires
collected source documents first, which is outside the foundation build.
