# data/staging — import inputs awaiting validation

CSV/YAML files prepared for the manual importer (adapters/manual.py) before
they are validated and written to the database. `example_manual_import/`
holds clearly-labeled synthetic (TEST- prefixed) demonstration data only.
